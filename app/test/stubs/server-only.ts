// Test stub for Next.js's `server-only` marker. The real module throws when
// bundled into client code; under vitest we just want it to be a no-op so
// server modules can be imported and unit-tested directly.
export {};
